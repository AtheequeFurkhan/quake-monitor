name: Hourly Seismic Update

on:
  schedule:
    - cron: '0 * * * *' # Runs at minute 0 of every hour
  workflow_dispatch:      # Allows manual triggering for testing

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write     # Important: Allows the bot to commit changes

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'

      - name: Run Data Fetcher
        run: go run main.go

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add README.md
          
          # Check if there are changes before committing to avoid empty commit errors
          if [[ -n $(git status -s) ]]; then
            timestamp=$(date -u)
            git commit -m "Update earthquake data: $timestamp"
            git push
          else
            echo "No changes in earthquake data."
          fi